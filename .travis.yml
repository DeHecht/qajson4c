dist: trusty
sudo: false
language: cpp
compiler:
  - gcc
install:
  - pip install pytest pytest-travis-fold install nose-parameterized
before_script:
  - mkdir build
  - cd build
  - cmake ..
script:
  - make
  - ./build/bin/unit-tests
  - py.test ../src-ftest/sample-test.py
