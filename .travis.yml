dist: trusty
sudo: false
language: cpp
compiler:
  - gcc
install:
  - pip install -e .
  - pip install pytest-travis-fold
before_script:
  - mkdir build
  - cd build
  - cmake ..
script:
  - make
  - ./build/bin/unit-tests
  - py.test ../src-ftest/sample-test.py
